export const someUtilityFunction = (input: any): any => {

    return input;
};

export const anotherUtilityFunction = (param1: string, param2: number): string => {
 
    return `${param1} - ${param2}`;
};